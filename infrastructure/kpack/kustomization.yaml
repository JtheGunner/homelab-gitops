apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# release.yaml = vendored kpack core (CRDs, controller, webhook, 'kpack' namespace).
# Fetch it per the runbook (see README), then commit.
resources:
  - release.yaml
  - cluster-stack.yaml
  - cluster-store.yaml
  - cluster-builder.yaml
